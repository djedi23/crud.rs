name: Release

on:
  push:
    tags:
      - "v*"

  release:
    types: [created]

jobs:
  publish-to-crateio:
    name: Publishing to crate.io
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package: [ crud, crud-derive, crud-api, crud-api-derive, crud-api-endpoint, crud-tidy-viewer, crud-auth, crud-auth-bearer, crud-auth-no-auth ]
    continue-on-error: true
    steps:
      - uses: actions/checkout@master
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: publish
          args: --token ${{ secrets.CARGO_API_KEY }} --allow-dirty -p ${{ matrix.package }}
